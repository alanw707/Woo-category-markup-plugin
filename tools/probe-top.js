const { chromium } = require('playwright');(async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:390,height:900}});await p.goto('https://shop.3wdistributing.com/',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForLoadState('load',{timeout:30000}).catch(()=>{});await p.waitForTimeout(3000);const rows=await p.evaluate(()=>Array.from(document.querySelectorAll('body *')).map(el=>{const r=el.getBoundingClientRect(); return {tag:el.tagName.toLowerCase(),id:el.id,cls:String(el.className).slice(0,120),x:Math.round(r.x),y:Math.round(r.y),w:Math.round(r.width),h:Math.round(r.height),txt:(el.textContent||'').trim().replace(/\s+/g,' ').slice(0,100)};}).filter(e=>e.y>=0&&e.y<140&&e.w>10&&e.h>5).slice(0,80));console.log(JSON.stringify(rows,null,2));await b.close();})().catch(e=>{console.error(e.stack);process.exit(1)});