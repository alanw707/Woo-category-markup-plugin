const { chromium } = require('playwright');(async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:390,height:900}});await p.goto('https://shop.3wdistributing.com/?bust=point',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForLoadState('load',{timeout:30000}).catch(()=>{});await p.waitForTimeout(3000);const d=await p.evaluate(()=>{let el=document.elementFromPoint(28,75); const arr=[]; for(let i=0;el&&i<8;i++,el=el.parentElement){const r=el.getBoundingClientRect(); const s=getComputedStyle(el); arr.push({tag:el.tagName.toLowerCase(),id:el.id,cls:String(el.className),rect:{x:r.x,y:r.y,w:r.width,h:r.height},font:s.fontSize,color:s.color,z:s.zIndex,text:(el.textContent||'').trim().replace(/\s+/g,' ').slice(0,200)});} return arr;});console.log(JSON.stringify(d,null,2));await b.close();})()