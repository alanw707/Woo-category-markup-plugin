const { chromium } = require('playwright');(async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:390,height:1200}});await p.goto('https://shop.3wdistributing.com/',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForLoadState('load',{timeout:30000}).catch(()=>{});await p.waitForTimeout(3000);const d=await p.evaluate(()=>Array.from(document.querySelectorAll('body [class*=qlwapp], body [id*=qlwapp]')).map(el=>{const r=el.getBoundingClientRect();const s=getComputedStyle(el);return {tag:el.tagName.toLowerCase(),id:el.id,cls:String(el.className),rect:{x:Math.round(r.x),y:Math.round(r.y),w:Math.round(r.width),h:Math.round(r.height)},pos:s.position,z:s.zIndex,display:s.display,text:(el.textContent||'').trim().replace(/\s+/g,' ').slice(0,100)}})); console.log(JSON.stringify(d,null,2)); await b.close();})()